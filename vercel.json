{
  "installCommand": "[ -d frontend ] && (cd frontend && npm install) || npm install",
  "buildCommand": "[ -d frontend ] && (cd frontend && npm run build && mkdir -p ../dist && cp -r dist/* ../dist/) || npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
